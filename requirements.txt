# Core dependencies
streamlit>=1.40.0
requests==2.32.3
beautifulsoup4==4.12.3
lxml>=5.2.2

# Embeddings (local, no API key needed)
sentence-transformers>=3.0.1
torch>=2.9.0

# Vector store
chromadb>=0.5.3

# LLM — uses direct requests to Groq API (no SDK needed)
# mistralai removed — switched to Groq

# Utilities
python-dotenv==1.0.1
